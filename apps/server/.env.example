# Copy to .env for local development (all values shown are the defaults).
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
# Absolute path to the built web client; set in production so the server also
# serves the SPA. Leave unset in dev — Vite serves the client on :5173.
# WEB_DIST=
